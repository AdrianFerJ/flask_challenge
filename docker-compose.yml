version: '3.7'

services:

  server:
    build:
      context: ./flask-api
      dockerfile: Dockerfile
    volumes:
      - './flask-api:/usr/src/app'
    ports:
      - 5001:5000
    environment:
      - FLASK_APP=project/__init__.py
      - FLASK_ENV=development

